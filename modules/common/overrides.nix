{
  # Common overrides that are useful across all systems
  commonOverrides = final: prev: {
    # Add common package overrides here that work on all systems
  };

  # Linux-specific overrides
  linuxOverrides = final: prev: {
    # Add Linux-specific package overrides here
  };

  # Darwin (macOS)-specific overrides
  darwinOverrides = final: prev: {
    # Add Darwin-specific package overrides here
  };

  # x86_64-specific overrides
  x86_64Overrides = final: prev: {
    # Add x86_64-specific package overrides here
  };

  # aarch64-specific overrides
  aarch64Overrides = final: prev: {
    # Add aarch64-specific package overrides here
  };
}
